name: Deploy Migrations to Staging

on:
  push:
    branches:
      - dev
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-22.04

    env:
      SUPABASE_ACCESS_TOKEN: sbp_3b4d649f4bc844674988648e4b6bfaa66116d485
      SUPABASE_DB_PASSWORD: Aa@0388162714
      STAGING_PROJECT_ID: dkkovannqhafjwyvirdb

    steps:
      - uses: actions/checkout@v3

      - uses: supabase/setup-cli@v1

      - run: |
          supabase link --project-ref $STAGING_PROJECT_ID
          supabase db push
